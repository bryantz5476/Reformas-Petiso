[build]
  command = "npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @workspace/reforma-luxury run build"
  publish = "artifacts/reforma-luxury/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
